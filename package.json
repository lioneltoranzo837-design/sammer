{
  "name": "sammer",
  "private": true,
  "type": "module",
  "scripts": {
    "build:ts": "tsc -p tsconfig.build.json",
    "test": "node --test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  },
  "dependencies": {
    "nostr-tools": "^2.23.5",
    "ws": "^8.21.0"
  }
}
