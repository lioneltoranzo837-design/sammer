{
  "name": "sammer",
  "private": true,
  "type": "module",
  "scripts": {
    "build:ts": "tsc -p tsconfig.build.json",
    "test": "node --test",
    "typecheck": "npm run typecheck:strict && npm run typecheck:runtime",
    "typecheck:runtime": "tsc -p tsconfig.runtime.json --noEmit",
    "typecheck:strict": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
